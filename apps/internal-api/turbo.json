{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "passThroughEnv": [
        "COOKIE_DOMAIN",
        "DATABASE_URL",
        "NEXT_PUBLIC_API_URL",
        "VERCEL",
        "VERCEL_EXPERIMENTAL_BACKENDS"
      ]
    }
  }
}
